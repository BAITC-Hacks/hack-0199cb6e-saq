# Проверка данных по PLAN.md §7

Дата подготовки: 23.09.2026. Ветка: `friend/data-readme`.

**Статус: по пунктам 1, 2, 4 и 5 внесены результаты предыдущей ручной сверки пользователя. Интеграционный прогон программы не выполнен; пункты 3 и 6 остаются непроверенными. Незавершённые проверки не блокируют передачу документа по решению пользователя.**

Источники разделены: ожидания взяты из [PLAN.md §7](PLAN.md) и [SPEC_DATA.md §7](SPEC_DATA.md); результаты ручной сверки переданы пользователем 23.09.2026 (дата самой сверки не указана). Codex исходные файлы не читал. Эти сведения нельзя представлять как результаты текущего запуска программы.

## Условия и фактически выполненная проверка

- Прочитаны PLAN.md, SPEC_DATA.md и readme_prompt.md; сопоставлены шесть пунктов чек-листа.
- Реальные файлы Codex не открывал, не загружал и не анализировал. По текущему поручению внесены только переданные пользователем результаты его ручной сверки; исключение из запрета на доступ к файлам для этого не требуется.
- В текущем checkout `git ls-files '*.py' 'pyproject.toml' 'uv.lock' 'docs/AGENTS.md'` не вернул файлов. Корневой README описывает репозиторий как пакет плана без кода. `Get-Command uv` не нашёл команду. Исполнимый путь загрузки и расчёта здесь не подтверждён.
- Синтетический прогон также не выполнен: генераторы и тесты в этом checkout отсутствуют. Проверка синтетики в любом случае не подтверждала бы состояние реальных файлов.

## 1. Загрузка 12 файлов и отчёт качества

**Факт / статус:** отдельные характеристики исходных данных подтверждены пользователем при предыдущей ручной сверке. Загрузка 12 файлов программой и формирование отчёта качества здесь не проверены.

Переданные пользователем результаты:

- Дубли кодов в MOQ ИЭК: 1, код `270400035_`.
- Дубли кодов в «Путь ИЭК»: 7 кодов — `220400107_` … `220400113_`.
- Неизвестный SKU LOOP: код `130200305_`, 210 000 шт, дата 09.06.2025.
- Сверка SE: IMT35100 (`030200192_`), август 2025 — 372 295 против 50 757. Пользователь передал эту пару под обозначением «SE ≈×2», но отношение указанных значений составляет ≈7,33. Пара чисел сохранена без исправления; она не подтверждает коэффициент ≈×2 для данного SKU и месяца. Принадлежность каждого значения конкретному источнику пользователем не уточнена.

Ожидание по спецификации:

- Распознаны все 12 файлов: по шесть на Systeme Electric и ИЭК — MOQ, транзакции, месячные остатки, месячные продажи, сезонность, рабочая таблица / товар в пути.
- Пустые количества транзакций: 13 SE и 18 ИЭК; строки исключены с `TX_EMPTY_QTY`.
- Итоговые строки и подзаголовки исключены; «Заказ покупателя» и «Приходная накладная» не считаются расходными продажами (`TX_DOC_TYPE_EXCLUDED`).
- Дубликаты кодов: один в MOQ ИЭК и семь в «Путь»; присутствует `DUP_CODE`.
- Чужой SKU `130200305_` (LOOP) попадает в карантин `TX_UNKNOWN_SKU`, а не в потребность ИЭК.
- Сверка транзакций с месячными продажами показывает известное расхождение SE примерно ×2 (ориентир §7 SPEC_DATA — 2,1), ИЭК примерно ×1 (0,92–1,05); июнь 2025 ИЭК до исключения LOOP — примерно ×2 (2,03).

Что зафиксировать после прогона: число распознанных профилей, агрегированные количества предупреждений, число исключённых/карантинных строк, отношения сумм по поставщику и месяцу с указанием этапа очистки. Не смешивать показатели до и после карантина LOOP.

## 2. Регулярный крупный покупатель

**Факт / статус:** пользователь подтвердил регулярный паттерн IMT35100 (`030200192_`): крупные строки объёмом 7 200–64 800 шт присутствуют каждый месяц из последних 12, при 200–440 транзакциях в месяц. По результатам ручной сверки это не разовый заказ. Конкретные границы 12-месячного периода не указаны. Расчёт программы не запускался; присвоение кода причины и отсутствие вычитания движком пока не проверены.

Ожидание: IMT35100, код `030200192_`, имеет повторяющиеся почти ежемесячно крупные строки. Они получают `REGULAR_LARGE_BUYER` и не вычитаются как разовые.

Что ещё проверить локально в программе: код причины и отсутствие вычитания регулярного крупного объёма. Повторяемость по месяцам подтверждена ручной сверкой пользователя. Зафиксировать итог «соответствует / не соответствует» без копирования накладных и значений продаж.

## 3. Восстановление спроса при stockout

**Факт / статус:** не проверено; расчёт не запускался.

Ожидание: для проверяемых позиций с хотя бы одним месяцем нулевого начального остатка восстановление положительно и не превышает cap, предусмотренный SPEC_ENGINE и параметрами запуска.

Что проверить локально: исходный и восстановленный спрос, величину добавки, фактический cap и причины отсутствия восстановления, если такие случаи есть. В протокол передать число проверенных позиций, число положительных восстановлений и число нарушений cap. Значение cap не подбирать и параметры движка в этой задаче не менять.

## 4. Товар в пути и ETA

**Факт / статус:** пользователь подтвердил по исходным данным две поставки для ВА47-29 16А (`010500006_`): 18 000 (ПП, до 01.10) + 12 000 (УТ-8233, до 30.09). Год в сообщении не указан; в спецификации это 2026 год. Отражение поставок в проекции программы не проверено.

Ожидание: ИЭК ВА47-29 16А, код `010500006_`, имеет две поставки: 18 000 по ПП с ETA 01.10.2026 и 12 000 по УТ-8233 с ETA 30.09.2026. Обе отражены в дневной проекции в соответствующие даты, без двойного учёта.

Что проверить локально: отдельный учёт документов и дат, отсутствие поступления раньше ETA, соответствие изменения проекции ожидаемым поставкам. Зафиксировать статус проверки двух поставок.

## 5. Единицы закупки

**Факт / статус:** по ручной сверке пользователя у ИЭК найдено 7 кабельных позиций, закупаемых бухтами по 305 м, вместо 6 в плане. Пользователь считает разницу некритичной и просит уточнить её при мердже. Пересчёт единиц, округление и рекомендации программы не проверены; сведения по 54 упаковочным позициям остаются ожиданием спецификации.

Ожидание: шесть кабельных позиций переводятся из метров в бухты по 305 м; 54 позиции «упаковками/штуками» — из штук в упаковки с коэффициентом конкретного SKU. Заказ выражен в единицах закупки с учётом MOQ и кратности.

Что проверить локально: единицу продажи, единицу закупки, коэффициент пересчёта и округление каждой позиции в согласованном перечне. Плановый объём — 60 позиций (6 + 54); фактический перечень нужно уточнить с учётом найденных 7 кабельных позиций. Коэффициент упаковки нельзя заменять коэффициентом кабеля. Зафиксировать число проверенных позиций и число ошибок отдельно для двух групп.

## 6. Разумность итогов

**Факт / статус:** не проверено; итоги и оценка закупщика отсутствуют.

Ожидание: человек оценивает долю строк к заказу, суммы по поставщикам и число `critical`. При необходимости владелец движка меняет только параметры `config.py` и повторяет расчёт.

Что зафиксировать после прогона: долю строк к заказу с явным знаменателем, суммы по каждому поставщику с валютой и покрытием ценами, число `critical`, решение закупщика и использованные параметры. Отсутствующие цены не представлять как нулевую стоимость.

Числовые пороги «разумности» в PLAN.md §7 не заданы; автоматический PASS по этому пункту невозможен без оценки человека.

## Заполнение после локального прогона

| Поле | Результат |
|---|---|
| Проверяющий и дата | Пользователь; результаты предыдущей ручной сверки переданы 23.09.2026, дата самой сверки не указана |
| Версия кода / commit | Не заполнено |
| Дата среза, идентификатор набора и run_id | Не заполнено |
| Параметры расчёта | Не заполнено |
| Пункты 1–6: статус и агрегированные доказательства | Пункты 1, 2, 4 и 5: сведения пользователя приведены выше; интеграционный прогон не выполнен. Пункты 3 и 6: не проверено. Незавершённые проверки не блокируют передачу документа по решению пользователя |
| Решение закупщика по п. 6 | Не заполнено |

Исходные таблицы, накладные, персональные данные и построчные логи в этот документ не включать. Ожидания спецификации не заменяют измерения.

## Открытые вопросы

- `TODO(spec):` уточнить источники пары 372 295 / 50 757 и область применимости обозначения «SE ≈×2»: для переданного SKU и месяца отношение ≈7,33. Это не блокирует сохранение переданных значений.
- `TODO(spec):` при мердже уточнить перечень кабелей ИЭК: по ручной сверке пользователя 7 позиций в бухтах 305 м, по плану — 6. Расхождение не блокирует текущую передачу по решению пользователя.
- `TODO(spec):` при последующей интеграционной проверке дополнить результаты работы программы. Пункты 3 и 6 оставлены без изменений; незавершённые проверки не блокируют текущую передачу по решению пользователя. Документ пока не является актом успешной приёмки MH1–MH5.
